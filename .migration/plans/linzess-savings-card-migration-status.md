# Migration Status — Linzess Savings Card Pages

## Original Migration Scope (4 pages)

| # | URL | Status | File |
|---|-----|--------|------|
| 1 | `https://www.linzess.com/savings-card` | ✅ Redesigned | `content/savings-card-hero.plain.html` |
| 2 | `https://www.linzess.com/savings-card/terms` | ❌ Not present | Was imported originally but lost during revert |
| 3 | `https://www.linzess.com/savings-card/activate` | ❌ Not present | Was imported originally but lost during revert |
| 4 | `https://www.linzess.com/savings-card/savings` | ❌ Not present | Was 403 error during import (CloudFront block) |

## What Exists Now

- **`content/savings-card-hero.plain.html`** — The redesigned savings card landing page (created manually with all sections: utility bar, page title, registration form, callout, eligibility cards, steps, FAQ accordion, ISI)

## What's Missing

The 3 subpages (`/terms`, `/activate`, `/savings`) that were originally imported by the bulk import script are no longer present. These were generated by running:
```
run-bulk-import.js --import-script import-savings-card-subpage.bundle.js --urls urls-savings-card-subpage.txt
```

The import scripts and bundled versions were not recreated during reconstruction (they require the bundler tool and live site access to execute).

## Checklist

- [x] Main savings card landing page migrated (`/savings-card` → `content/savings-card-hero.plain.html`)
- [ ] Re-import `/savings-card/terms` (requires running bulk import with subpage script)
- [ ] Re-import `/savings-card/activate` (requires running bulk import with subpage script)
- [ ] Re-import `/savings-card/savings` (was 403 — may still be blocked)
- [ ] Generate `import-savings-card-subpage.js` import script
- [ ] Bundle the import script (`.bundle.js`)
- [ ] Execute bulk import for the 3 subpage URLs

---

**Summary:** 1 of 4 pages is present. The remaining 3 subpages need to be re-imported by running the content import workflow (requires Execute mode and live site access).
