# Switch to Formulary-Lookup Branch in AEM Coder

## Steps

Open this URL directly in your browser — it will open the DA editor on the `formulary-lookup` branch:

```
https://da.live/#/edit/Nishant-Adobe/abbvie-eds-poc/formulary-lookup/
```

If you're already in AEM Coder and need to switch:

1. Look for a **branch icon** or **branch name** in the top bar (it currently shows `develop`)
2. Click it to open the branch switcher dialog
3. Type `formulary-lookup` in the search box
4. Select it and click **"Switch Branch"**

If the Switch Branch button doesn't respond, close the dialog and use the direct URL above instead.

## Checklist

- [ ] Open `https://da.live/#/edit/Nishant-Adobe/abbvie-eds-poc/formulary-lookup/` in browser
- [ ] Verify the branch shows as `formulary-lookup` (not `develop`)
- [ ] Navigate to the page you want to edit (e.g., `Formulary-lookup` or `before-after-rinvoq`)

---

*No code changes or Execute mode needed — just open the URL.*
