# Chart Block — Revised File List (Block Already Exists — Confirmed)

## Confirmed in Codebase ✅

**Chart block exists** with all base files + UE registration:

| File | Status |
|------|--------|
| `blocks/chart/chart.css` | ✅ Exists |
| `blocks/chart/chart.js` | ✅ Exists |
| `blocks/chart/_chart.json` | ✅ Exists |
| `blocks/chart/block-config.js` | ✅ Exists |
| `blocks/chart/abbvie/_chart.css` + `chart.css` + `block-config.js` | ✅ Exists — DO NOT TOUCH |
| `blocks/chart/botox/_chart.css` + `chart.css` + `block-config.js` | ✅ Exists — DO NOT TOUCH |
| `blocks/chart/rinvoq/_chart.css` + `chart.css` + `block-config.js` | ✅ Exists — UPDATE |
| `component-definition.json` → `"id": "chart"` (line 1227) | ✅ Registered |
| `component-models.json` → `"id": "chart"` (line 8606) | ✅ Model exists |
| `component-filters.json` → `"id": "chart"` (line 249) | ✅ Filter exists |

**Missing brand folders:**
- `blocks/chart/skyrizi-hcp/` — ❌ Does not exist — NEED TO CREATE
- `blocks/chart/mavyret/` — ❌ Does not exist — NEED TO CREATE

---

## What Needs to Be Done

Only work on **3 brand folders**: `rinvoq` (update), `skyrizi-hcp` (create), `mavyret` (create)

Plus: register `chart` in `models/_section.json` so it appears in UE section block list.

---

## New Files to Create (4)

| # | File Path | Purpose |
|---|-----------|---------|
| 1 | `blocks/chart/skyrizi-hcp/_chart.css` | Skyrizi brand partial — `@import '../chart.css'` + brand selectors |
| 2 | `blocks/chart/skyrizi-hcp/block-config.js` | Skyrizi block config |
| 3 | `blocks/chart/mavyret/_chart.css` | Mavyret brand partial — `@import '../chart.css'` + brand selectors |
| 4 | `blocks/chart/mavyret/block-config.js` | Mavyret block config |

## Existing Files to Modify (6)

| # | File Path | What Changes |
|---|-----------|-------------|
| 5 | `blocks/chart/rinvoq/_chart.css` | Update with brand-specific selectors using `var(--chart-*)` |
| 6 | `styles/tokens.css` | Add `--chart-*` default declarations (if not already there) |
| 7 | `styles/skyrizi-hcp/tokens.css` | Add `--chart-*` brand overrides |
| 8 | `styles/rinvoq-hcp/tokens.css` | Add `--chart-*` brand overrides |
| 9 | `styles/mavyret/tokens.css` | Add `--chart-*` brand overrides |
| 10 | `models/_section.json` | Add `"chart"` to section components list so block appears in UE |

## Auto-Generated by Gulp (3)

| # | File Path | Generated From |
|---|-----------|----------------|
| 11 | `blocks/chart/skyrizi-hcp/chart.css` | Compiled from `_chart.css` |
| 12 | `blocks/chart/rinvoq/chart.css` | Recompiled from `_chart.css` |
| 13 | `blocks/chart/mavyret/chart.css` | Compiled from `_chart.css` |

---

## DO NOT TOUCH

- `blocks/chart/chart.css` — base CSS already exists
- `blocks/chart/chart.js` — block JS already exists
- `blocks/chart/_chart.json` — UE model already exists
- `blocks/chart/block-config.js` — config already exists
- `blocks/chart/abbvie/` — don't touch
- `blocks/chart/botox/` — don't touch
- `component-definition.json` — chart already registered (line 1227)
- `component-models.json` — chart model already exists (line 8606)
- `component-filters.json` — chart already in allowed components (line 249)

---

## Total: 13 Files

| Category | Count |
|----------|-------|
| New files to create | 4 |
| Existing files to modify | 6 |
| Auto-generated (gulp) | 3 |
| **Grand total** | **13** |

---

## Checklist

- [ ] Create `blocks/chart/skyrizi-hcp/_chart.css`
- [ ] Create `blocks/chart/skyrizi-hcp/block-config.js`
- [ ] Create `blocks/chart/mavyret/_chart.css`
- [ ] Create `blocks/chart/mavyret/block-config.js`
- [ ] Modify `blocks/chart/rinvoq/_chart.css` — update brand selectors
- [ ] Modify `styles/tokens.css` — add `--chart-*` defaults (if missing)
- [ ] Modify `styles/skyrizi-hcp/tokens.css` — add `--chart-*` overrides
- [ ] Modify `styles/rinvoq-hcp/tokens.css` — add `--chart-*` overrides
- [ ] Modify `styles/mavyret/tokens.css` — add `--chart-*` overrides
- [ ] Modify `models/_section.json` — add `"chart"` to section components list
- [ ] Run `npx gulp createBrandCSS` — generates/updates 3 compiled files
- [ ] Verify: zero `:root{}` in compiled output
- [ ] Verify: zero lint errors
- [ ] Verify: chart appears in UE block palette
- [ ] Verify: renders in UE, localhost, aem.live

## Status

Confirmed: chart block and UE registration already exist. Adding `chart` to `models/_section.json` for UE section visibility. Only brand-level CSS for 3 brands + section registration. 13 files total. Implementation requires Execute mode.
